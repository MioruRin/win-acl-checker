<Page
    x:Class="AclChecker.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="32,8,32,32">
        <StackPanel Spacing="20">

            <TextBlock Text="关于" Style="{StaticResource TitleTextBlockStyle}"/>

            <!-- 应用信息卡片 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                <StackPanel Spacing="12">
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <Image Source="Assets/app_icon.png" Width="48" Height="48"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ACL 权限排查修复工具" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="版本 1.0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="扫描选定软件的全链路目录权限，检测并修复沙盒/受限用户无法访问的问题。"
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <TextBlock Text="基于 Windows App SDK (WinUI 3) 构建"
                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}"/>
                </StackPanel>
            </Border>

            <!-- 功能说明 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                <StackPanel Spacing="12">
                    <TextBlock Text="主要功能" Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <Grid ColumnSpacing="16" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon Grid.Column="0" Grid.Row="0" Glyph="&#xE721;" FontSize="20"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="扫描目录链 — 从根目录到目标文件的完整权限链路分析"/>

                        <FontIcon Grid.Column="0" Grid.Row="1" Glyph="&#xE7BA;" FontSize="20"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="检测异常 — 对比 Windows 默认权限标准"/>

                        <FontIcon Grid.Column="0" Grid.Row="2" Glyph="&#xE8A7;" FontSize="20"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" Text="一键修复 — 自动修复权限继承和访问控制"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 权限标准说明 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                <StackPanel Spacing="12">
                    <TextBlock Text="Windows 默认权限标准" Style="{StaticResource SubtitleTextBlockStyle}"/>

                    <Grid ColumnSpacing="8" RowSpacing="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <FontIcon Grid.Column="0" Grid.Row="0" Glyph="&#xE73E;" Foreground="#4CAF50" FontSize="16"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="继承" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Grid.Row="0" Text="启用 (inheritance:e)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <FontIcon Grid.Column="0" Grid.Row="1" Glyph="&#xE73E;" Foreground="#4CAF50" FontSize="16"/>
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="Users" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Grid.Row="1" Text="读取 + 执行 (RX)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <FontIcon Grid.Column="0" Grid.Row="2" Glyph="&#xE73E;" Foreground="#4CAF50" FontSize="16"/>
                        <TextBlock Grid.Column="1" Grid.Row="2" Text="SYSTEM" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Grid.Row="2" Text="完全控制 (F)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <FontIcon Grid.Column="0" Grid.Row="3" Glyph="&#xE73E;" Foreground="#4CAF50" FontSize="16"/>
                        <TextBlock Grid.Column="1" Grid.Row="3" Text="Administrators" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="2" Grid.Row="3" Text="完全控制 (F)" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
